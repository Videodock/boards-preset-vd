import { takeLatest, all } from 'redux-saga/effects';
import * as {{pascalCased}}Types from '../../redux/{{pascalCased}}/{{pascalCased}}.types';
import * as {{pascalCased}}Sagas from './{{pascalCased}}.sagas';

export default function * () {
  return yield all([
  ]);
}
